# v2.1 self-contained inference pipeline

Direct Ollama endpoint only: `http://192.168.20.62:11434`; every request performs `/api/tags` preflight and requires `qwen3.5:4b`. No HOLDOUT execution is performed by module construction.
